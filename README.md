## インタラクティブGr. データストラテジスト研修Lv.2で配布用レポジトリです。

### 1. Git clone "git -URL " でローカルレポジトリを作成
### 2. docker build -t "image name":"tag name" でdocker imageを作成
### 3. docker run --rm -it -p 8888:8888 -v:/work   "image name":"tag name"でコンテナを起動し、ブラウザのURLバーからhttp://localhost:8888 にアクセスしてjupyterlabを利用